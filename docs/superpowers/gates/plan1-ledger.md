# 계획 1 원장 사본 (SDD ledger, 판결 R1~R88)

원본: .superpowers/sdd/2026-09-02-seat-changer-v2-plan1-foundation/progress.md (작업 공간 삭제 전 보존). 게이트 기록은 task-01~17.md 참조.

```
# SDD ledger — plan: docs/superpowers/plans/2026-09-02-seat-changer-v2-plan1-foundation.md

Spec: docs/superpowers/specs/2026-09-02-seat-changer-v2-design.md (reachable)
Playbook: docs/superpowers/plans/2026-09-02-execution-playbook.md (tiers, G9 GPT QA)
Branch: v2 @ d5ccabb (start). Workspace: C:\Users\Public\seat-changer

## Pre-flight scan (2026-09-02 13:52)

| Pair / Task | Produces vs consumes | Found |
|---|---|---|
| T1 → T3 | T1 placeholder globals.css / T3 replaces with tokens | consistent |
| T1 → T2 | T1 tsconfig `include: scripts` / T2 test imports `scripts/scan-emoji.mjs` from TS | tsc needs `allowJs` for .mjs import → Ruling R1 |
| T1 → T9/T10/T13 | tsconfig / tests import `legacy/js/**.js` | same: allowJs → R1 |
| T4 → T5/T6 | PushPin/Tape props | consistent |
| T6 → T7 | NoteSeat data-attrs / e2e locators | consistent (`[data-size]` locator is redundant, harmless) |
| T8 → T9/T10 | LayoutSettings type / getSeatPositions(settings) | consistent |
| T10 → T11/T12 | getLayout, SeatPosition / posMap, adjacency | consistent |
| T11 → T12 | shuffle(arr, rng), buildRuleLookup, precomputeGenderSeats | consistent |
| T12 → T13 | randomizeSeats(data, {rng,yieldToUI}) / golden | consistent; capacity check differs from legacy by design (documented in T12) |
| T14 → T16 | createClassRegistry API | consistent |
| T15 → T16 | loadClassData, importClassJSON | consistent |
| T15 self | test "범위 밖 값": rows -1 expected 5 | legacy `Math.max(1, Math.min(12, parseInt(-1) || 5))` = 1 → Ruling R2 |
| T16 self | zundo partialize vs data-wide restore | plan already names the fallback (pause/resume); OK |
| T17 self | e2e expects 22 students | depends on fixture; plan says adjust |
| T1 self | pinned npm versions may not exist | plan allows same-major latest, log in gates |

Ruling R1: Task 1 tsconfig.json adds `"allowJs": true, "checkJs": false` — needed for .mjs and legacy JS imports in tests — cost if wrong: tsc may surface loose-JS errors in scripts/; fix by excluding.
Ruling R2: Task 15 test expects `out.layoutSettings.rows` = 1 for input -1 (legacy clamps, spec says "legacy importJSON과 동일") — cost if wrong: one assertion.

## Progress
Note: custom agents impl-*/spec-reviewer/quality-reviewer not registered in this session (need restart) → fallback: general-purpose + model param + effort stated in prompt (per cron instruction).
Task 1: dispatched (impl tier M → general-purpose/sonnet, effort high in prompt), BASE d5ccabb
Task 1: implementer DONE (commits b7a4611, 0ccd4ac; 5/5 tests; typecheck+build ok; gate not runnable until T2). Reviews dispatched in parallel: spec(sonnet/high), quality(opus/medium), G9 GPT adversarial (--base d5ccabb → task-1-g9.md). Review package: review-task1-slim.diff (10.7KB; full 300KB package kept as review-d5ccabb..0ccd4ac.diff)
Task 1: spec review ✅ (no findings; rename R100 ×27, R1 applied, pins exact, trailers verified)
Task 1: quality review Approved w/ Important#1 (tsconfig.node.json inert: no references, no noEmit — plan-mandated). Ruling R3: fix it — add references to tsconfig.json and noEmit to tsconfig.node.json — cost if wrong: tsc may complain about composite/refs, trivial to revert. Minors deferred: resolveRoute prefix over-capture (/presentation), App render coverage 1/3, test:cov empty-set failure until core exists, .gitignore lacks coverage/ test-results/ playwright-report/ .vercel, isolatedModules missing, npm audit dev-only (vitest UI GHSA-5xrq, vite fs.deny Windows GHSA-93m4 → bump vite within 7.x in a later task). Waiting on G9 before fix round 1.
Task 1: G9 GPT adversarial = needs-attention, 4 findings. Rulings: R4 accept alias fix (vite resolve.alias '@'→src + alias regression test). R5 accept legacy CJS boundary: add legacy/package.json {type:commonjs} (only file added under legacy/; source untouched), verify once that node legacy/build.js regenerates identical bundle.js — no permanent test (YAGNI). R6 accept routing: exact-segment match for /present and /dev/cork (trailing slash tolerated), and v1 hash compat: pathname '/' + hash '#student' → present (spec 1장 보완; App gets optional hash prop) + tests for /presentations, /dev/corks, /#student, trailing slash, query. R7 accept dependency bump: vite→latest 7.x (≥7.3.5), vitest & @vitest/coverage-v8 → latest 3.x, regenerate lockfile, rerun typecheck/test/build; record versions. Cost if wrong: R6 changes brief code (routing) — small; R7 could shift vitest behavior — tests will show. Fix round 1 → resume implementer.
Task 1: fix round 1 commit 8e3013f (15/15; vite 7.3.6 vitest 3.2.7 audit 0). Ruling R3 amended: noEmit in referenced composite config is TS6310-invalid → CLI --noEmit per config (Vite template pattern) accepted. Bundle regen 1-line diff (blank line) — Ruling R8: acceptable, legacy bundle frozen; golden tests import modules not bundle. Re-review dispatched (sonnet) + G9 re-run (--base 0ccd4ac).
Task 1: fix round 1/5 (6 addressed, 0 open; commits 0ccd4ac..8e3013f). Re-review out-of-scope note: CLI --noEmit reliance is latent fragility (add comment later) — deferred minor.

## User directive (2026-09-02 14:2x)
User: 사용자 승인이 필요한 지점은 최후순위로 미룸. 내일 이어서 작업. → Rule: never block on AskUserQuestion; park every user-decision item under '## Pending user decisions' below and continue with the best ruling. Push/deploy remain parked (never done autonomously).

## Pending user decisions
- Task 17 Step 4: 이 PC의 실제 프로덕션 localStorage 데이터로 수동 로드 확인(개인정보 — 사용자가 직접 브라우저에서 복사). 컨트롤러는 픽스처 E2E까지만 수행하고 이 항목은 사용자에게 넘긴다.
- 최종: v2 브랜치 푸시·Vercel 프리뷰 배포·베타·프로덕션 승격.

Task 1: G9 re-review approve (no material findings).
Task 1: complete (commits d5ccabb..8e3013f + gates f0e07c5, review clean)
Task 2: dispatched (tier S → general-purpose/sonnet, effort medium), BASE f0e07c5
Task 2: implementer DONE_WITH_CONCERNS (7ed3f8e; 18/18; gate green). Ruling R9: concern 3 (CLI scan skips *.test.*) rejected — weakens G4; tests stay scanned, fixture string concatenated. Concerns 1 (react-hooks configs.recommended[0].rules) and 2 (Node globals block for scripts/**/*.mjs) accepted. Fix round 1 sent before review (correctness concern).
Task 2: fix round 1 done (6376a62). Reviews dispatched: spec(sonnet), quality(sonnet — Ruling R10: S-tier tasks use sonnet for quality review too; diff 6.8KB mechanical), G9 (--base f0e07c5 → task-2-g9.md). Package review-task2.diff.
Task 2: spec review ✅. Minor deferred: core restriction glob is src/core/**/*.ts only (add *.tsx) — will fold into any fix round or Task 8 dispatch.
Task 2: quality review Needs fixes — Important: TEXT_SYMBOL_ALLOW block ranges let ✅❌⚡☀▶Ⓜ↔ through (plan-mandated regex). Ruling R11: explicit allow set + \p{Emoji_Presentation}|\p{Extended_Pictographic} check; minors folded: IMAGE_IMPORT_RE i-flag + dynamic import. Fix round 2/5 sent to implementer. G9 still running.
Task 2: G9 needs-attention, 3 high. Rulings: R12 accept (decode \u escapes; fixtures via String.fromCodePoint). R13 accept (image ref regex any path w/ ext, ?url/#, new URL, CSS url; walk adds js/jsx/cjs; image FILES under src banned → 'image-file'); rejected: AST/CSS parser (YAGNI for a gate). R14 accept (core glob ts/tsx/js/jsx; patterns incl. subpaths & relative components/store/pages; no-restricted-syntax ImportExpression + globalThis; lintText regression tests). Amendment sent into fix round 2.
Task 2: fix round 2 part 1 (R11) committed eff96a8 (22/22, gate green). Amendment R12-R14 in progress (same implementer).
Task 2: fix round 2/5 amendment committed 3d3d189 (37/37; gate green). Re-review (sonnet) + G9 re-run (--base 6376a62) dispatched.
Task 2: fix round 2/5 re-review: 4/4 addressed, no new breakage. Deferred minors: decode false-positive on regex literals escaping disallowed codepoints (intended); globalThis double-report (cosmetic).
Task 2: G9 r2 needs-attention (3). Rulings: R15 accept barrel-dir patterns + tests; R16 accept backslash-parity decode + tests; R17 REJECT string-concat image bypass (regex gate = accidental use only; G7 network check covers runtime) → header comment + checklist note. Fix round 3/5 sent (same implementer).
Task 2: fix round 3/5 committed 416c806 (44/44). Re-review (sonnet) + G9 r3 (--base 3d3d189) dispatched.
Task 2: fix round 3/5 re-review: 3/3 addressed, no breakage (reviewer briefly created+removed a temp test file despite read-only; tree clean — note for future reviewer prompts: forbid temp files).
Task 2: G9 r3 approve.
Task 2: complete (commits f0e07c5..416c806 + gates 02a0dc6, review clean; 3 fix rounds)
Task 3: dispatched (tier S → general-purpose/sonnet, effort medium), BASE 02a0dc6
Task 3: implementer DONE (c8eea93; 14 token tests; gate 58 green). Reviews dispatched: spec(sonnet), quality(sonnet, R10), G9 (--base 02a0dc6).
Task 3: spec review ✅ (verbatim). Minor deferred: .texture-wood border-color #5E3A1B untokenized (from brief).
Task 3: quality Approved w/ 2 Important: (a) report claims bg-cork absent from dist — false (pages already use it) → Ruling R18: report-only inaccuracy, correct in gates record, no code loop; (b) tokens.test lowercases whole CSS so --color-corK typo passes (plan-mandated) → Ruling R19: fix test to match exact-case name + lowercase value. Minors deferred: @utility vs @layer note, url() regex i-flag/data:, scroll-behavior, __dirname style. Waiting G9 to batch fix round 1.
Task 3: G9 needs-attention (5). Rulings: R20 accept contrast (allowed pairs test; cork text=ink; placeholders text-ink; carry to T6: disabled NoteSeat text on cork must not be paper-on-transparent → use paper/60 bg + ink; T7: titles on cork use text-ink). R21 accept 2-color focus ring (ink outline + paper box-shadow) + 3:1 tests. R22 accept @utility. R23 accept animation:none (carry to T6/T7/plan2: resting state must be visible without animation). R24 partial: token test exact-case name now; compiled-output verification deferred to T7 Playwright computed-style checks (body bg, texture gradient, font-family). Fix round 1/5 sent.
Task 3: fix round 1/5 committed b70d121 (71 tests). Re-review (sonnet) + G9 r2 (--base c8eea93) dispatched.
Task 3: fix round 1/5 re-review: 5/5 addressed, no breakage. Note: tailwind content scanner picks class strings from test files (report found 'texture-cork' literal in tokens.test.ts causing false 'used'); TeacherPage now consumes texture-cork.
Task 3: G9 r2 approve.
Task 3: complete (commits 02a0dc6..b70d121 + gates 331b14f, review clean; 1 fix round)
Task 4: dispatched (tier S → general-purpose/sonnet, effort medium), BASE 331b14f
Task 4: implementer DONE (776a319; 3 tests; gate 74). Ruling R25: diffs under ~150 lines get ONE combined spec+quality reviewer (sonnet) — cost/turn proportionality; G9 still runs. Reviews dispatched (--base 331b14f).
Task 4: combined review Approved (no findings; minors: trailing double space in className, keyof typeof typing).
Task 4: G9 needs-attention (2 medium). Rulings: R26 accept pointer-events-none on decorations + unit test; R27 accept positioned-parent contract in JSDoc + shared DECOR_BASE const; browser bbox/click-through tests deferred to T7 Playwright (carry into T7 dispatch). Fix round 1/5 sent.
Task 4: fix round 1/5 committed de68b36 (77 tests). Re-review (sonnet) + G9 r2 dispatched.
Task 4: fix round 1/5 re-review: 2/2 addressed, no breakage.
Task 4: G9 r2 approve.
Task 4: complete (commits 331b14f..de68b36 + gates cc58d1d, review clean; 1 fix round)
Task 5: dispatched (tier M → general-purpose/sonnet, effort high), BASE cc58d1d
Task 5: implementer DONE (1826a11; 85 tests; danger light-stop contrast 3.047 — thin margin, noted). Reviews dispatched: spec(sonnet), quality(opus), G9 (--base cc58d1d).
Task 5: correction — diff is 107 lines → R25 applies: ONE combined reviewer (sonnet) dispatched, not spec+quality pair.
Task 5: combined review Approved. Minors deferred: contrast test hardcodes wood stops (promote to tokens later); {...rest} after type=button allows override (brief-inherited).
Task 5: G9 needs-attention (1 high, valid): danger md text needs 4.5:1 across gradient; my R20 thresholds were wrong. Ruling R28: darken texture-wood light stop (≥4.6 vs #FFF3D6, e.g. #96612D), contrast matrix size×stop reading stops from CSS, WOOD_TEXT constant guarded by test. Fix round 1/5 sent.
Task 5: fix round 1/5 committed 2a8853a (88 tests; light stop #916532 4.63:1). Re-review (sonnet) + G9 r2 dispatched.
Task 5: fix round 1/5 re-review: R28 addressed, no breakage.
Task 5: G9 r2 approve.
Task 5: complete (commits cc58d1d..2a8853a + gates b8f9e61, review clean; 1 fix round)
Task 6: dispatched (tier M → general-purpose/sonnet, effort high), BASE b8f9e61
Task 6: implementer DONE (6498cbd; 99 tests). Ruling R29: mute on paper-2/paper-3 <4.5 (4.25/4.46) → seat number uses ink; contrast test documents the miss (mute allowed only on paper). Reviews dispatched.
Task 6: spec review ✅ (controller miscounted tests 7→6; no gap).
Task 6: quality (opus) Needs fixes — Critical: opacity-50 empty label 3.0:1; Important: no tests for R20/R29, dead tab stops for handler-less buttons, highlight ring gold 1.9:1 + no data attr, ChalkBoard bg literal untested. Rulings R30: empty label = text-ink font-normal (no opacity) + test; R31: class assertions for disabled look + seat number ink; R32: native disabled when no handler for the state (RTL getByRole still finds); R33: highlight ring-ink + data-highlight + test; R34: export BOARD_BG='#26443C', contrast pair test, comment; R35: rename local 'disabled'→isRemoved; R36: empty aria-label → '{n}번 자리 (빈 자리)' (label-in-name) with test updated. Deferred minors: ring vs focus box-shadow, extra prop tests. Waiting G9 to batch.
Task 6: G9 needs-attention (2 high, 2 medium) — overlaps quality review; extra: empty+name mismatch → R37 accept (render 빈 자리 regardless of name). Visual check of 5-char name at lg → T7. Fix round 1/5 sent (R30-R37).
Task 6: fix round 1/5 committed 994fde5 (110 tests). Re-review (sonnet) + G9 r2 dispatched.
Task 6: fix round 1/5 re-review: 8/8 addressed, no breakage. Deferred: ring-ink overrides focus box-shadow when highlighted+focused (cosmetic); removed seat without onRestore keeps '되살리기' label while disabled (acceptable); contract for later tasks: pass handlers for interactive seats (R32).
Task 6: G9 r2 needs-attention (1 medium, valid). Ruling R38: removed seat w/o onRestore → visible '삭제된 자리', aria-label '(삭제됨)'; seatLabel gains canRestore param. Fix round 2/5 sent.
Task 6: fix round 2/5 committed 025004c (112 tests). Re-review (sonnet) + G9 r3 dispatched.
Task 6: fix round 2/5 re-review: R38 addressed, no breakage (minor: '삭제된 자리' not substring of '(삭제됨)' — non-operable, deferred).
Task 6: G9 r3 approve.
Task 6: complete (commits b8f9e61..025004c + gates 356391a, review clean; 2 fix rounds)
Task 7: dispatched (tier L → general-purpose/opus, effort medium), BASE 356391a

## Session paused 2026-09-02 ~20:00 (user: 내일 이어서)
Task 7: INTERRUPTED mid-implementation (agent stopped). Working tree has UNCOMMITTED partial work: M tsconfig.node.json, ?? e2e/, ?? playwright.config.ts, ?? e2e-red.log (scratch log — do not commit). No task-7-report.md yet. Known blocker found by implementer: tsconfig.node.json (no DOM lib, no skipLibCheck) fails typecheck on Playwright types → Ruling R39 (provisional): add "skipLibCheck": true and "lib": ["ES2022","DOM"] to tsconfig.node.json (or move playwright.config.ts/e2e under tsconfig.json include only) — implementer to verify tomorrow. Resume: fresh implementer (opus/medium) with task-7-brief.md + the same dispatch rulings (R20 texture-cork/text-ink, R32 handlers, click-through probe, computed-style checks, two screenshots); tell it to inspect the partial files first and continue, not restart.

## Session resumed 2026-09-03
Custom agents impl-*/spec-reviewer/quality-reviewer now registered → use them directly.
Task 7: re-dispatched (tier L → impl-l opus/medium), BASE 356391a, fresh implementer told to inspect partial tree (tsconfig.node.json, e2e/dev-cork.spec.ts, playwright.config.ts) and continue; R39 provisional carried.
Task 7: implementer DONE_WITH_CONCERNS (aefd8ea, 3e46850; vitest 112, e2e 1 passed, gate green). Ruling R40: keep scripts/run-e2e.mjs (PWTEST_CACHE_DIR pinned to ASCII path in repo) — Playwright crashes 0xC0000409 with Korean TEMP; dev tooling only, honors pre-set env — cost if wrong: one extra script. R39 finalized: skipLibCheck only (e2e/ already typed via tsconfig.json DOM). Extra "강조 좌석" card accepted (renders R33 highlight state for G7). Reviews dispatched: spec(spec-reviewer), quality(quality-reviewer), G9 (--base 356391a → task-7-g9.md). Package review-356391a..3e46850.diff (14KB).
Task 7: G7 visual check by controller (test-results/dev-cork.png, 1920×1080 vs checklists/ui.md): no emoji (lucide check/undo/dice only); h1 ink on cork; seat text ink on paper; "황보아리랑" fits on lg and sm seats without clipping; pins centered on card tops; tapes on seats; highlight seats show ink ring; chalkboard/podium chalk text large; disabled seats dotted; no image requests (e2e). Not visible in screenshot: focus ring (Tab), reduced-motion — covered by unit tests (T3/T6), deferred to e2e later. Verdict: PASS.
Task 7: spec review ❌ — Step 4 (G7 via review subagent) and Step 5 (gates/task-07.md) not done by implementer. Ruling R41: both steps are controller-owned in this session's process (as Tasks 1-6: gate record = controller commit "docs(gates)", G7 judged by controller from screenshot) — not a code gap; no fix loop for these — cost if wrong: none (record written before completion).
Task 7: quality review Needs fixes — Important ×3. Rulings: R42 accept click-through probe rewrite: assert getComputedStyle(tape/pin).pointerEvents==='none' AND click a point inside tapeBox∩seatBox (falsifiable) with seat scoped to the grid section (data-testid), not .first(). R43 accept reuseExistingServer → opt-in only: `reuseExistingServer: !!process.env.PW_REUSE_SERVER` (default false; stale preview on 4173 fails loudly) — cost if wrong: dev must kill leftover server. R44 accept split: separate test() per concern (render+screenshots first, computed styles, geometry/click-through, name clipping+no-image) so screenshots exist even when a later assertion fails. Folded minors (same lines rewritten): document.fonts.check('16px Gaegu') for font actually loaded; drop arbitrary width>=150 (keep scrollWidth/containment). Deferred minors: pin tolerance comment, run-e2e child.on('error')/SIGINT, R23 reduced-motion e2e, skipLibCheck widening.
Task 7: G9 needs-attention (1 high, 3 medium). Rulings: R45 REJECT pixel baseline (toHaveScreenshot): cross-OS font/AA rendering variance makes committed PNG baselines flaky and G7 is by design a reviewed-screenshot gate; ACCEPT falsifiable substitute — assert computed color of h1 == ink token and highlight seat's computed ring/outline color == ink (R20/R33) — cost if wrong: layout regressions caught only at G7 review. R46 accept font loading: await document.fonts.ready, assert document.fonts.check('16px Gaegu') true, register page.on('requestfailed') for font/stylesheet resources and fail the test if any; measure name dimensions only after. R42 amended: exhaustive seat state check — every [data-cork=note-seat] enabled except the non-restorable removed one (disabled); Tape AND PushPin computed pointer-events none. R47 accept run-e2e hardening: treat empty PWTEST_CACHE_DIR as unset; verify final cache path is ASCII on win32, else exit with a message asking for an explicit ASCII PWTEST_CACHE_DIR. Fix round 1/5 sent to implementer (R42-R47 + folded minors).
Task 7: fix round 1/5 (7 addressed, 0 open; commits 3e46850..15e2931). Re-review clean; note: e2e now genuinely requires Google Fonts network access (intended). G9 r2 running.
Task 7: G9 r2 needs-attention (3 medium, all valid). Ruling R48 accept all: (a) shared helper — install font/stylesheet requestfailed tracking before goto and assert Gaegu FontFace loaded in EVERY font-dependent test (screenshot, computed-style, name-clipping) since each Playwright test gets its own page; (b) `reuseExistingServer: process.env.PW_REUSE_SERVER === '1'` (sentinel only; "false"/"0" must not reuse) + a small unit test of the parsing helper; (c) run-e2e.mjs resolves the cache dir to an absolute path against repoRoot BEFORE the ASCII check and passes that absolute path to the child. Cost if wrong: trivial. Fix round 2/5 sent to implementer.
Task 7: fix round 2/5 committed 8b18535 (vitest 121 incl. e2e-env.test.ts 9; e2e 4/4; gate green). Implementer concerns: (1) src/test/eslint-core-boundary.test.ts first case runs 3.1–4.8s vs 5000ms default timeout → one flaky gate failure observed. Ruling R49: raise that test's timeout to 20_000 (vitest per-test timeout arg) — flaky gate blocks every downstream task; cost if wrong: none. To be folded into the next dispatch (fix round 3 if any, else Task 8). (2) tsconfig.json `references` + tsconfig.node.json `composite` removed after TS7016→TS6307→TS6305 chain when importing the e2e-env helper. Ruling R50: accept — typecheck runs both configs via CLI (R3 amended), references only serve `tsc -b` which this repo does not use; cost if wrong: a future `tsc -b` user must re-add references. Re-review (sonnet) + G9 r3 (--base 15e2931) dispatched.
Task 7: fix round 2/5 re-review: 3/3 addressed, R50 holds, no breakage.
Task 7: G9 r3 needs-attention (1 medium, valid): font check passes if any Gaegu face loaded (400 ok, 700 404 → HTTP 4xx is not requestfailed). Ruling R51: accept core fix — helper takes weight/text, asserts `document.fonts.load('700 28px Gaegu', '황보아리랑')` returns ≥1 face AND no Gaegu face has status 'error'; REJECT the 404-stub regression test (route-mocking Google Fonts in e2e is heavier than the gate it protects; YAGNI) — cost if wrong: a broken helper goes unnoticed until a real font failure. G9 loop cap (3, per playbook) reached → after fix round 3, scoped re-review only, no G9 r4. R49 (eslint-core-boundary timeout 20s) folded into this round. Fix round 3/5 sent.
Task 7: fix round 3/5 committed f841512 (e2e 4/4, vitest 121, gate green). Residual limit noted: missing 700 @font-face declaration still passes via weight matching (deferred, synthetic-bold detection too heavy). Re-review (sonnet) dispatched.
Task 7: fix round 3/5 re-review: 2/2 addressed, no breakage.
Task 7: complete (commits 356391a..f841512 + gates 59c40f6, review clean; 3 fix rounds; G9 cap reached, 1 residual deferred). Phase 0 gate PASS.
Task 8: dispatched (tier M → impl-m sonnet/high), BASE 59c40f6
Task 8: implementer DONE (3baf818; 7 new tests, 128 total; gate green; no zod4 adaptations). Ruling R52: 202-line diff but pure transcription of brief code → ONE combined reviewer (spec-reviewer sonnet/high) per R25 spirit; G9 runs. Package review-59c40f6..3baf818.diff.
Task 8: combined review Approved (spec ✅ verbatim vs brief, legacy models.js/store.js caps cross-checked). Minors (plan-mandated, deferred): LIMITS.MAX_HISTORY=5 unused while schema caps history at 10 (legacy importJSON slice 10) — Ruling R53: leave; Task 16 (store) must verify the legacy push cap for assignmentHistory/groupHistory and either use LIMITS.MAX_HISTORY or fix its value — carry into Task 16 dispatch. `as unknown as z.ZodType<ClassData>` cast — plan-mandated, accepted; Task 15 migration tests exercise the shape. Waiting G9.
Task 8: G9 needs-attention (1 high, 1 medium). Ruling R54 (high, partial): ACCEPT superRefine on fixedSeats — seatIndex unique AND studentName unique, else parse fails (+ tests); REJECT roster-membership / seat-in-range / not-disabled checks at schema level — they need the layout seat count (Task 9/10 core/layouts) and would invert the layering; those invariants belong to Task 12 (constraint check) and Task 15 (migration normalizes: drop invalid/duplicate fixed seats deterministically BEFORE parse) — carry into both dispatches. Cost if wrong: v1 data with duplicate fixed seats fails parse until Task 15 dedupes. Ruling R55 (medium): ACCEPT canonical Assignment keys — `^(0|[1-9]\d*)$` + Number.isSafeInteger; any non-canonical key or collision → ctx.addIssue (parse failure, no silent drop); tests for '', ' 1 ', '1e2', '01', '-1', 1e21. REJECT seat-range check (layout dependency). Note: JS objects serialize integer keys canonically, so real v1 data is unaffected. Fix round 1/5 sent.
Task 8: fix round 1/5 committed fff43b4 (10 new tests, 138 total, gate green). Re-review (sonnet) + G9 r2 (--base 3baf818) dispatched.
Task 8: fix round 1/5 re-review: 2/2 addressed, no breakage (dead collision branch in Assignment refine — harmless). G9 r2 approve.
Task 8: complete (commits 59c40f6..fff43b4 + gates ebaec63, review clean; 1 fix round)
Task 9: dispatched (tier L → impl-l opus/medium), BASE ebaec63
Task 9: implementer DONE (2722aa4; 30 layout tests, 168 total; gate green). 187-line port diff → ONE combined reviewer (R52 spirit); G9 dispatched (--base ebaec63). Package review-ebaec63..2722aa4.diff.
Task 9: combined review Approved (bodies line-for-line vs legacy verified; 3 settings cases × 3 layouts). Minors deferred: gridPositions export (brief-sanctioned, Task 10 uses), param names pos1/pos2. Waiting G9.
Task 9: G9 needs-attention (2 medium, both valid). Ruling R56: (a) brief's `SeatPosition.group?: number` conflicts with legacy group-layout output `groupIndex` — spec (legacy identity) wins over plan text → rename to `groupIndex?: number`; carry into Task 10 dispatch (brief 10 may also say `group`). (b) identity tests add boundary settings 1×1, 1×12, 12×1, 12×12 and a non-empty disabledSeats case for all three layouts vs legacy; manhattan/chebyshev compared directly against legacy layout-engine.js exports. Cost if wrong: trivial. Fix round 1/5 sent.
Task 9: fix round 1/5 committed 2f9d8e5 (77 layout tests, 215 total, gate green; no legacy diff at boundaries — ushape columns=1 overlap reproduced as-is). Re-review (sonnet) + G9 r2 dispatched.
Task 9: fix round 1/5 re-review: 2/2 addressed (excess-property check verified), no breakage. G9 r2 approve.
Task 9: complete (commits ebaec63..2f9d8e5 + gates e1e6390, review clean; 1 fix round)
Task 10: dispatched (tier L → impl-l opus/medium), BASE e1e6390
Task 10: implementer DONE_WITH_CONCERNS (b67750d; 43 new tests, 258 total; gate green). Concern: brief note "legacy models.getTotalSeats == groupLayout.getSeatCount" is false — legacy models.js counts ushape as columns*rows (30 vs layout 16) and has a groupPositions.length*groupSize fallback the layout lacks. Traced legacy: models.getTotalSeats is used only by teacher-screen pre-checks and fixed-seat-editor; student-screen uses layout-based getTotalSeatsForLayout and randomizeSeats derives seats from the layout. Ruling R57: keep the brief's layout-based getTotalSeats (implementer's choice) — it is the truth the randomizer uses; models.js is a latent legacy bug (ushape overcount would let the UI pass a roster the randomizer cannot seat). Divergence pinned by tests; carry into Task 12/13 (golden tests compare randomizeSeats, not models.getTotalSeats) and Plan 2 UI (pre-check uses v2 getTotalSeats). Cost if wrong: a v1 ushape class with more students than 16 seats now gets an earlier, correct error. Reviews dispatched: spec(spec-reviewer), quality(quality-reviewer), G9 (--base e1e6390).
Task 10: spec review ❌ — third getTotalSeats divergence: legacy models.js groupCount*fallback has no upper clamp, group-layout clamps groupCount to 20 → differs for groupCount>20; not tested/documented. Ruling R58: accept — pin with a legacy-anchored test (groupCount 25) and note that ClassDataSchema caps groupCount at 20 so valid v2 data never hits it; add ushape case without disabledSeats (minor folded). Waiting quality + G9 to batch fix round 1.
Task 10: quality review (opus) Approved, minors only. Folded into fix round 1 (same test file as R58): third divergence pinned (groupCount 25 / 21 sizes) with "스키마상 도달 불가" note; ushape divergence test asserts literal numbers per side; group identity cases get string labels. Deferred minors: `py!`/`sizes[g]!` non-null assertions (legacy-identical behavior), unreachable `?? examLayout` fallback in getLayout (schema guarantees LayoutType; revisit in Task 15 if unknown layoutType can reach it), untested `customDesks || []` branch. Waiting G9.
Task 10: G9 needs-attention (1 medium, valid): getTotalSeats subtracts disabledSeats.length without dedupe/range check (schema allows [999], [0,0]) → under-count. Legacy models.js has the same behavior, but per R57 v2 getTotalSeats is the seat truth. Ruling R59: accept — non-custom layouts subtract the size of the Set of indexes with 0 <= i < raw; tests for 1-seat+[999] → 1, 3-seat+[0,0] → 2, plus a pinned divergence-from-models.js note; carry to Task 12 (randomizer must apply the same filtered disabled set) and Task 15 (migration dedupes/range-filters disabledSeats). Cost if wrong: v2 pre-check accepts rosters legacy rejected; consistent with the randomizer. Fix round 1/5 sent (R58 + R59 + folded minors).
Task 10: fix round 1/5 committed d33ef2f (126 layout tests, 264 total, gate green; gate record draft swept into commit by git add -A — harmless, finalized in docs commit later). Re-review (sonnet) + G9 r2 (--base b67750d) dispatched.
Task 10: fix round 1/5 re-review: all addressed (R58, R59, minors; input change [0,7]→[0,5]/[0,1] justified), no breakage. Waiting G9 r2.
Task 10: G9 r2 approve (first r2 run died with the killed wrapper; re-run).
Task 10: complete (commits e1e6390..d33ef2f + gates f07f60a, review clean; 1 fix round)
Task 11: dispatched (tier XL → impl-xl opus/high), BASE f07f60a
Task 11: implementer DONE_WITH_CONCERNS (af91bf3; 41 new tests, 305 total; gate green; core coverage 98.8% lines). Rulings: R60 precomputeGenderSeats returns null for genderRule 'none' (brief interface; legacy never returns null) → Task 12 MUST treat null as "all availableSeats for every student" — carry into Task 12 dispatch. R61 buildAdjacencyMap follows legacy (Manhattan-1 on row/col, not layout.distance) → adjacency [1,6] (brief allowed); lookup.ts does not import layouts. Dead `else` in mixed gender (slack1==slack2 always) preserved as legacy. Dropped `|| 'none'` / `|| {}` fallbacks: acceptable (schema guarantees). Reviews dispatched: spec(spec-reviewer), quality(quality-reviewer opus), G9 (--base f07f60a).
Task 11: spec review ✅ (bodies verified vs legacy; shuffle constant derived from hand-copied legacy shuffle; Set-forced single loop in same branch is inert). Waiting quality + G9.
Task 11: quality review (opus) Approved, minors only: near-vacuous disabledSeats adjacency test (positions never filtered), tautological mulberry32 not-all-zero assertion, no custom/group adjacency case (quantized row/col collapse quirk), no empty students/seats boundary per gender branch, shared Set references across students (legacy aliasing) → Ruling R62: Task 12 must treat GenderSeatSets sets as read-only (carry into dispatch). Minors folded only if G9 opens a fix round; else deferred. Waiting G9.
Task 11: G9 needs-attention (1 medium, valid): student names used as plain-object keys → 'toString'/'constructor'/'__proto__' break buildRuleLookup (push on inherited fn) and buildNameToSeatMap. Ruling R63: accept — lookups use Object.create(null) + Object.hasOwn (type stays Record<string,…>), regression tests with reserved names; carry to Task 12: every `studentGenders[name]`/lookup read must use hasOwn-safe access, and Task 15 migration keeps names as-is (no legacy semantic change). Quality minors folded into this round (same test file): fix vacuous disabledSeats adjacency test, drop tautological mulberry32 assertion, add custom-layout adjacency case, add empty students/seats boundary per gender branch, document GenderSeatSets sets as shared/read-only (R62). Fix round 1/5 sent.
Task 11: fix round 1/5 committed 3a2d147 (53 helper tests, 317 total, gate green; R63 also caught __proto__ prototype replacement in gender.ts result). Re-review (sonnet) + G9 r2 (--base af91bf3) dispatched.
Task 11: fix round 1/5 re-review: 6/6 addressed, no logic drift, no breakage. Waiting G9 r2.
Task 11: G9 r2 needs-attention (1 medium): student named '__proto__' loses gender through ClassDataSchema z.record parse → unknown gender → all seats. Ruling R64: REJECT for Task 11 (schema is Task 8's; helper handles the key correctly; degradation is graceful, no crash; name is unrealistic) — parked; Task 15 migration may sanitize reserved names. Cost if wrong: one absurdly-named student escapes the gender rule.
Task 11: complete (commits f07f60a..3a2d147 + gates 9e9077a, review clean; 1 fix round; 1 parked)
Task 12: dispatched (tier XL → impl-xl opus/high), BASE 9e9077a
Task 12: implementer DONE_WITH_CONCERNS (98e8ef4; 46 new tests, 363 total; gate green; randomizer coverage 99.5% lines). Rulings: R65 accept legacy `dist <= minDistance` rejection in verifyAssignment (brief fixture {0,12} was infeasible on 6 cols; fixture moved to seat 18, boundary pinned) — verify must not be looser than the randomizer. R66 accept gender (mirrors checkGenderConstraintFast) + capacity violation kinds beyond legacy's fixed+separation (brief union requires); legacy '↔' replaced by '-' (Extended_Pictographic fails G4 scan — proven). R67 accept optional `clock` in RandomizeOptions (deterministic timeout tests; Task 13 may use). Legacy bugs ported faithfully (recorded): fixed seats on disabled/out-of-range seats ignored by randomizer but flagged by verify; duplicate fixed seats (now unreachable via R54 schema); dead params in checkConstraints. Reviews dispatched: spec(spec-reviewer), quality(quality-reviewer opus), G9 (--base 9e9077a).
Task 12: quality review (opus) Approved, minors: verify header comment overstates "[] for every success" (fixed seat on disabled seat: randomizer drops, verify flags — legacy-faithful) → pin with a test + soften comment; timeout vs infeasible share identical detail → append elapsed fact; custom layout + disabledSeats capacity mismatch (getTotalSeats ignores disabled for custom, tryAssignment excludes them) noted, likely unreachable; test-local groupIndexOf duplicates getGroupIndex. Folded into a fix round if spec/G9 open one; else deferred. Waiting spec + G9.
Task 12: spec review ✅ (function-by-function vs legacy; only sanctioned deviations; no-layout uses direct layouts[] lookup instead of getLayout fallback — correct vs brief sample). ⚠️ rng-consumption-order identity deferred to Task 13 golden tests (esp. gender-null/allSeats snapshot path). Minor: own() helper duplicated across gender/constraints/verify → later cleanup. Waiting G9.
Task 12: G9 needs-attention (1 high, 2 medium). Ruling R70 (high, accept as intentional divergence): legacy fallback condition `assignmentHistory.length > 0` ignores lastAssignment-only state although checkHistoryConstraint uses lastAssignment → v2 condition becomes `(assignmentHistory.length > 0 || lastAssignment?.mapping)`; minimal test (1 student/1 seat, lastAssignment same seat → ok via historyFallback). Legacy bug; pinned as divergence; carry to Task 13 (golden tests must pin this scenario as an expected v1≠v2 case). Cost if wrong: one golden case differs — documented. Ruling R68 (medium, accept): verifyAssignment skips fixed seats whose seatIndex is disabled or >= seat count, mirroring tryAssignment (v2 verify already extended by R66); regression test: randomize result with a fixed seat on a disabled seat verifies to []; carry to Plan 2 UI: surface "고정석이 비활성 좌석" as a config warning instead. Ruling R69 (medium, REJECT for Task 12): legacy-oracle RNG-order golden tests ARE Task 13; not duplicated here. Quality minors folded (same files): verify header comment softened, timeout `detail` carries elapsed/deadline fact, test-local groupIndexOf replaced by exported getGroupIndex or literals. Fix round 1/5 sent.
Task 12: fix round 1/5 committed 8a8a7f0 (103 randomizer tests, 367 total, gate green). Note for Task 13: exclude/pin R59 disabled-capacity and R70 lastAssignment-only scenarios as v1≠v2. timedOut flag covers driver loop only (deadline crossed inside final backtrack → plain detail) — accepted minor. Re-review (sonnet) + G9 r2 (--base 98e8ef4) dispatched.
Task 12: fix round 1/5 re-review: 4/4 addressed, no breakage (cosmetic: verify.ts header list numbered 1/3/2 — deferred). Waiting G9 r2.
Task 12: G9 r2 needs-attention (2 medium, both valid, both small). Ruling R71 accept: after the attempt loop (either pass) set `timedOut = clock() > deadline` so a deadline crossed inside the final backtrack is reflected in detail (no rng/behavior change); test: maxAttempts 1 + clock that crosses deadline mid-backtrack → detail has the suffix. Ruling R72 accept minimal: hasHistory requires `assignmentHistory.length > 0 || Object.keys(lastAssignment?.mapping ?? {}).length > 0` (empty mapping does not arm the fallback); REJECT the roster-overlap refinement (legacy never does this; over-engineering) — cost if wrong: an irrelevant old mapping still arms a harmless second pass. Fix round 2/5 sent; then re-review + G9 r3 (cap).
Task 12: fix round 2/5 committed 2884ef1 (106 randomizer tests, 370 total, gate green; R72 RED showed doubled rng burn 30 vs 15). Re-review (sonnet) + G9 r3 (--base 8a8a7f0, cap) dispatched.
Task 12: fix round 2/5 re-review: 3/3 addressed, no breakage. Waiting G9 r3 (cap).
Task 12: G9 r3 approve.
Task 12: complete (commits 9e9077a..2884ef1 + gates 828ce4d, review clean; 2 fix rounds)
Task 13: dispatched (tier XL → impl-xl opus/high), BASE 828ce4d
Task 13: implementer DONE (b152c14; 13 golden scenarios × 3 seeds all match legacy with zero v2 core edits; 4 intentional-divergence tests; 388 total; coverage src/core 99.3/92.2/100 vs thresholds 90/90/85/90; non-vacuity proven by perturbing shuffle → all fail). Concerns noted: timeout paths and failure paths not golden-compared (frozen clocks), groupHistory exclusion + historyExcludeCount 3 not in golden set (unit-tested in T12) → Ruling R73: accept as Phase 1 evidence; add groupHistory/excludeCount-3 golden scenarios in the fix round if any opens, else deferred to Plan 2 pre-beta. Reviews: combined (spec-reviewer sonnet/high) on the golden file; G9 as PHASE QA over Tasks 8-13 (--base 59c40f6, per playbook §Phase gate).
Task 13: combined review Approved (non-vacuous: v1 asserted non-null per combo; harness verified against legacy shuffle/yieldToUI source; only the golden file changed). Waiting G9 Phase 1 QA.
Task 13: G9 Phase 1 QA needs-attention (3 high) — all legacy-identical behaviors. Ruling R74 (carry): fixed-vs-fixed constraints unchecked (legacy same; v1 surfaced via verifyAssignment toast) → Task 16 store must call verifyAssignment after success and expose violations; Plan 2 fixed-seat editor pre-warns. Ruling R75 (carry): timeout does not abort recursion (legacy cadence) → keep for golden identity; Plan 2/3: propagate timeout tri-state + consider Worker. Ruling R76 (carry → Task 15): duplicate student names pass schema → add students uniqueness refine in Task 15 with deterministic dedupe (keep first) in migration/import; Task 16 setStudents dedupes. Cost if wrong: known legacy defects persist until Plan 2. No fix round.
Task 13: complete (commits 828ce4d..b152c14 + gates 968f352, review clean; 0 fix rounds; 3 carried). PHASE 1 GATE PASS.
Task 14: dispatched (tier S → impl-s sonnet/medium), BASE 968f352
Task 14: implementer DONE (62bed0c; 9 new tests, 397 total; gate green). Concerns: add() dedupes on untrimmed name (legacy quirk, ported); remove() `?? 1반` fallback for type safety. Reviews: combined (spec-reviewer sonnet/high, S tier R10/R25) + G9 (--base 968f352).
Task 14: combined review Needs fixes (2 Important): rename() uses `??` where legacy store.js:87 uses `||` (empty-string data → legacy substitutes defaults) — Ruling R77: match legacy (`||`) + test; duplicate() writes copied data under dataKey(newName.trim()) where legacy store.js:130 uses the untrimmed key (legacy bug: padded name loses the copy) — Ruling R78: KEEP the trimmed key as an intentional legacy-bug fix, disclose in report/deviation list + test (`duplicate('1반','  사본  ')` → data under dataKey('사본') equals source). Cost if wrong: none for stored v1 data. Folded minors: tests for rename collision, >50-char/whitespace names, duplicate of missing source, active() with ACTIVE absent; report line-mapping corrected. Waiting G9 to batch fix round 1.
Task 14: G9 needs-attention (1 high, 2 medium). Ruling R79 (high, partial accept): StorageAdapter.set/remove return boolean (brief says void — additive deviation; Task 16 consumers must check it), localStorageAdapter returns false on throw (quota); registry rename/remove/add/switchTo/duplicate return false when any write fails; rename/remove order = write target/metadata first, delete source LAST, and roll back the target key if metadata write fails; tests with a failing adapter (target write fails, metadata write fails). REJECT full transactional rollback beyond that (localStorage has no transactions). Cost if wrong: slight interface drift from brief. Medium #2 = R77 (already accepted). Medium #3 accept: migration test asserts adapter keys/values directly. Fix round 1/5 sent (R77, R78 disclosure, R79, folded minors).
Task 14: fix round 1/5 committed be70b46 (25 storage tests, 413 total, gate green). Extra disclosed deviation: duplicate() of a missing source → false (legacy would copy null). Re-review (sonnet) + G9 r2 (--base 62bed0c) dispatched.
Task 14: G9 r2 FAILED — Codex usage limit ("try again at 3:07 PM"). Ruling R80: G9 is blocked until ~15:07; for a scoped fix-round re-review the Sonnet re-review is sufficient (G9 r1 already examined the task) → Task 14 completes on the re-review verdict. Tasks 15-17: dispatch and review normally; run G9 when the limit resets (retry at 15:10+); if still unavailable, run one GPT Phase 2 QA over Tasks 14-17 afterwards and record it in task-17.md. Cost if wrong: a fix-round-only defect slips to the Phase 2 QA.
Task 14: fix round 1/5 re-review: 6/6 addressed, no breakage. Out-of-scope: duplicate() non-atomic on copy-write failure (entry left with default data) → carry to Task 16; writeRaw discards boolean → Task 16 checks adapter.set directly.
Task 14: complete (commits 968f352..be70b46 + gates 7f500cc, review clean; 1 fix round; G9 r2 skipped per R80)
Task 15: dispatched (tier L → impl-l opus/medium), BASE 7f500cc
Task 15: implementer DONE (7107a0e; 450 total; gate green). Controller miss: preflight Ruling R2 (rows -1 → legacy clamp 1, not brief's 5) was not carried into the dispatch → implementer followed the brief. R2 stands (legacy importJSON identity): fold into fix round 1 — columns/rows `max(1,min(12,parseInt||default))`, groupSize `max(2,min(8,..))`, groupSizes `max(1,min(8,..))`, minDistance per legacy; brief test expectation corrected to 1. Ruling R81: schema-forced shape validation (malformed desks/rules/gender entries dropped, history records with non-numeric timestamp dropped) accepted as v2-stricter-than-legacy, documented. Legacy parity via models.js only (store.js.importJSON has load-time side effects) accepted. Reviews dispatched: spec(spec-reviewer), quality(quality-reviewer opus). G9 deferred until Codex limit resets (~15:07).
Task 15: spec review PASS (field table produced; only R2-class deviations: negative values for columns/rows/groupSize/groupSizes go to default instead of legacy clamp; groupCount same; extra strictness (desks/positions/history/genders/groupLayoutMode/minDistance) is R81). Waiting quality to batch fix round 1 (R2 + any findings).
Task 15: quality review (opus) Needs fixes — 4 Important (all verified, all accepted, Ruling R82): (1) intField throws on object-valued numeric fields (null-prototype objects lack toString) → guard typeof number|string else fallback; (2) Number.isInteger vs schema safe-int → Number.isSafeInteger in seatIndex/groupIndex filters; (3) loadClassData('null'|'[]'|'1') returns ok:true+defaults → guard non-object → ok:false; (4) junk-input test lacks these shapes → add. Folded minors: export ASSIGNMENT_KEY_PATTERN from schema (single source), assert fixture __proto__ stripped, Korean error strings (no raw e.message), single stripDangerousKeys pass per load, replace tautological safeParse assertions with field-value asserts. Deferred: stripDangerousKeys type-honesty (brief signature), deep-recursion stack overflow (doc comment fix only). Fix round 1/5 sent (R2 + R82 + minors).
Task 15: fix round 1/5 committed 40dc5a2 (92 model+storage tests, 463 total, gate green; "intentional deviation" withdrawn — negatives now clamp like legacy). Re-review (sonnet) dispatched; G9 pending Codex reset.
Task 15: fix round 1/5 re-review: 6/6 addressed, no breakage (note: intField treats arrays as fallback — documented extension).
Task 15: complete (commits 7f500cc..40dc5a2 + gates 99901f8, review clean; 1 fix round; G9 deferred to Phase 2 QA per R80)
Task 16: dispatched (tier XL → impl-xl opus/high), BASE 99901f8
Task 16: implementer DONE (f6698de; 35 store tests, 498 total; gate green). Findings: nested zundo partialize overwrote data (students lost) → flat partialize + withUndoMerge middleware + pause/resume (both). R53 resolved: legacy cap is 5 == LIMITS.MAX_HISTORY. Carries to Plan 2 (Ruling R83): randomize/violations not in AppState (brief) — Plan 2 spec must place violations (likely a transient `lastViolations` slice) and call verifyAssignment (R74); recordAssignment(…, true) sets the legacy fallback notice, so caller clears it when violations exist; loadNotice is a single string (5 notices overwrite) → Plan 2 decides toast queue. Reviews: spec(spec-reviewer), quality(quality-reviewer opus); G9 when Codex resets.
Task 16: spec review PASS (AppState verbatim; undo merge strategy validated against brief risk; recordAssignment matches legacy student-screen 526-568 incl. cap 5; R79/R76/R59/R63 verified). Notes: isUndoSnapshot heuristic keyed on `layoutSettings` in partial (fragile if AppState grows — deferred with guard suggestion); useTemporal is reactive hook (Plan 2: call inside render); boot uses adapter.get directly (== readRaw). Waiting quality.
Task 16: quality review (opus) Needs fixes — Important 2 (Ruling R84 accept both): duplicateClass reports COPY_FAILED on plain name-collision (must compare registry.list() before/after); setGridSize persists unvalidated grid that loadClassData rejects on next boot (clamp to LIMITS.MIN_GRID..MAX_GRID or no-op). Folded minors: setUntracked try/finally; setStudents clears temporal (stale fixedSeats/genders in undo snapshots); isUndoSnapshot structural check (all six keys); dedupe-then-cap in setStudents (sanitize slices before dedupe); tests for duplicateClass notice, HISTORY_FALLBACK notice, notice-only change writes nothing, one write per mutation; drop unused adapter return in helper. Fix round 1/5 sent.
Task 16: fix round 1/5 committed 8d9c55b (47 store tests, 510 total, gate green). Note: setGridSize symptom corrected — loadClassData clamps (no reset); real defect was silent value change on reload + oversized in-memory grid. Plan 2 must use setGridSize (update(partial) unvalidated). Re-review (sonnet) dispatched.
Task 16: fix round 1/5 re-review: 6/6 addressed, no breakage (setStudents now clears temporal — intentional). Out-of-scope: registry.add untrimmed collision check (legacy quirk) → Plan 2 trims names.
Task 16: complete (commits 99901f8..8d9c55b + gates aac2004, review clean; 1 fix round; G9 deferred to Phase 2 QA)
Task 17: dispatched (tier M → impl-m sonnet/high), BASE aac2004
Task 17: implementer DONE (d5e8e56; gate 510, e2e 6 passed (dev-cork 4 + v1-data-load 2), src/core cov 99/92.7/98/99). Concern: TeacherPage forces boot re-save via `useEffect(() => update({}))` to satisfy the "key rewritten with schemaVersion 2" e2e assertion. Ruling R85: wrong layer — the store must write back ONCE at boot/class-load when `loadClassData` returns `migrated: true` (not when ok:false), so v1 data is upgraded on first load regardless of UI; remove the page hack; add a store unit test (boot with v1 fixture → adapter holds schemaVersion 2 JSON; boot with v2 → no write; corrupt → no write). Cost if wrong: one extra write per v1 class on first boot. Fix round 1/5 sent BEFORE review (correctness concern); combined review after.
Task 17: G7 controller check (test-results/teacher-diag.png): diag JSON on cork with ink text, h1 Gaegu, no emoji, v1 fixture loaded (classes [1반, 6-7], active 6-7, students 22, migration notice). PASS (temporary block).

## User directive (2026-09-03 14:4x)
User: 너무 오래 걸림. GPT 피드백을 전부 받아들이지 말고 실제 반영할 만한 것만 반영. → Ruling R86 (process): from now on (a) accept only findings that cause data loss / crash / wrong result / v1-compat break; everything else = 기각 or 보류 one-liner, no fix round; (b) at most ONE fix round per remaining item, no GPT re-reviews (r2/r3) at all; (c) Task 17: skip the separate combined review — the R85 fix re-check is done by the controller (gate + e2e run) ; (d) Phase 2 QA (GPT, once, after 15:07) and the final whole-branch review run IN PARALLEL; only real defects get one batched fix; (e) tag v2-phase2 after that.
Task 17: fix round 1/5 committed 8fc3c06 (store loadFor write-back once when migrated; SAVE_FAILED overrides MIGRATED; page hack removed; 515 tests; e2e 6). Per R86: no separate combined review; controller runs e2e as gate evidence; final whole-branch review (opus) dispatched at HEAD 8fc3c06 (base f0e07c5, 54 commits).
Task 17: controller e2e run at HEAD 8fc3c06: 6 passed (28.6s) — Phase 2 gate evidence.
Final review (opus) FIX FIRST: 1 actionable — lastAssignment/history timestamp outside Date range (|t|>8.64e15) survives migration and crashes recordAssignment (new Date(t).toISOString RangeError), losing the draw. Ruling R87: accept; guard at the migration choke point (isValidTimestamp for assignment/group records) + regression test; controller applies directly (one-line class fix, per R86 speed directive). 11 deferred items recorded in task-17.md. Gate at 8fc3c06: 515 tests green, golden 18/18.
Final review fix committed 989577b (516 tests, gate green). Waiting Phase 2 GPT QA (scheduled 15:08) → then tag v2-phase2.
Phase 2 GPT QA (Task 15-17, base 7f500cc): needs-attention 1 high — multi-tab stale full-state overwrite. Checked legacy: v1 store.js:220-234 has initSync (storage event on the active data key → reload + callback), so v2 lacking it is a real v1 feature regression with a data-loss path. Ruling R88: accept minimal parity — store gains `reloadFromStorage()` (re-read class list/active/data from the adapter, clear temporal, no write) and `attachStorageSync(target)` that listens for `storage` events on the active data key / CLASSES / ACTIVE and calls it; singleton attaches it under the window guard. REJECT revision/merge conflict detection (legacy has none; over-engineering for a single-teacher tool). One batched fix (impl-m), then controller gate check, gate record commit, tag.
R88 fix committed d6018f7 (521 tests, gate green). Controller e2e: 6 passed (21.1s).
Task 17: complete (commits aac2004..d6018f7 + gates, review clean per R86; Phase 2 GATE PASS)
PLAN 1 COMPLETE: Tasks 1-17 done, final review clean after R87, Phase 2 QA resolved by R88. Tag v2-phase2. Ledger archived to docs/superpowers/gates/plan1-ledger.md; workspace deleted.

```
